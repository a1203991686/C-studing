#include"stdio.h"
int main()
{
    int T;
    scanf("%d", &T);
    while(T--)
    {
        int M, i, j, k; //k代表层数
        scanf("%d", &M);
        for (i = 0; i < M; i++)
        {
            j = k = 1;
        }
    }
    return 0;
}